const { getActor } = require("./actor");
import { Ed25519KeyIdentity } from "@dfinity/identity";

const minter = Ed25519KeyIdentity.generate();
const user1 = Ed25519KeyIdentity.generate();
const user2 = Ed25519KeyIdentity.generate();

test('Mint a new ICRC7 collection canister', async () => {

})

test('Checks the Name of Icrc7 Collection Canister', async () => {

})

test('Checks the Symbol of Icrc7 Collection Canister', async () => {
    
})

test('Minting of the Nft', async () => {

})

test('Minting of the Nft, Should throw error (Unauthorized)', async () => {

})

test('Checks the Owner of the Newly Minted Nft', async () => {

})

test('Transfer of Token, should Succeed', async () => {

})

test('Transfer of Token, should throw error ()', async () => {
    
})

test('Transfer of Token, should throw error ()', async () => {
    
})

test('Transfer of Token, should throw error ()', async () => {
    
})

test('Transfer of Token, Should throw error ()', async () => {
    
})

test('Burning of the NFT', async () => {

})

test('Burning of the NFT, should throw error (Unauthorized)', async () => {

})